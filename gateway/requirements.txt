# Runtime + test deps in one file. For a real deployment the pytest lines
# would move to a separate dev image stage — see README "Tradeoffs".
fastapi>=0.115,<0.200
uvicorn[standard]>=0.30
asyncpg>=0.30
httpx>=0.27,<1.0
pydantic>=2.7

pytest>=8.0
pytest-asyncio>=0.23
